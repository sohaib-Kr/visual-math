---
const menuItems = [
  { id: 0, title: "Topology"},
  { id: 1, title: "Functional Analysis" },
  { id: 1, title: "Probability Theory" },
  { id: 1, title: "Algebra" },
];
---
<div class="w-full h-fit grid grid-rows-1 bg-[#310f70] relative z-[1]">
            
    <div class="grid overlap">
        <div id="svgContainer" class="overlap pt-[150px]">
        </div>
        <div class="overlap bg-[#310f70]" id="svgContainerMask"></div>
        <div class="bg-[#1e0943] overlap opacity-[0.4] grid">
        </div>
        <div class="overlap grid">
            <div class="justify-self-end self-end relative bottom-[300px] w-fit right-[100px] grid h-fit bg-white rounded-[40px]">
                <svg class="w-full h-[50px] overlap" id="arrowIcon">
                    <path fill='none' d="M 0 39 L 55 39 L 38 25 M 55 39 L 39 52" />
                </svg>
                <a href="#" id='seeLessonsButton' class="overlap text-[#1e0943] w-fit font-bold text-[25px] px-[40px] py-[10px] ">
                    See Lessons
                </a>
            </div>
        </div>
    </div>

    <div class="w-[56%] grid overlap grid-rows-1 pt-[350px] relative">
        <div class="grid overlap">
            <div class="ml-[100px] overlap font-[poppins] text-[white]">
                 <h2 id="title" class='text-[54px]  mb-[50px]'>Topology</h2>
                 <p id="description" class='text-[25px] leading-[50px] font-[merriweather]'>
                    Topology is a branch of mathematics that studies the properties of 
                     space that are preserved under continuous deformations, 
                     such as stretching or bending, but not tearing or gluing.
                    </p>
            </div>

        </div>
        <div class="justify-self-start relative bottom-[200px] left-[100px] h-fit overlap">
            {menuItems.map((item) => (
            <a href="#" data-id={item.id} class="text-[21px] rounded-[50px] m-[10px] bg-white px-[20px] py-[10px]">{item.title}</a>
              ))}
        </div>
    </div>
    
</div>